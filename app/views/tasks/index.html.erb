<h1>My Tasks 📋</h1>
<ul>
  <% @tasks.each do |task| %>
  <li>
  <input type="checkbox" <%= 'checked' if task.completed %> disabled>
  <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
<%= link_to '+ Add a new task', new_task_path, class: 'task-title' %>
